# Device validation report

Copy this file for a new Android handheld. Remove private hostnames, addresses, usernames, project/agent names, account details and device serials before sharing. Do not attach raw terminal screenshots or logs containing conversation text.

- APK version, source commit and SHA-256:
- Device model, firmware and Android API:
- Physical pixels, density, font scale and usable app bounds:
- WebView version and speech provider:
- Physical buttons tested and saved calibration behavior:
- D-pad KeyEvent/HAT duplication:
- Home → agent → touch/D-pad reading → reviewed response → Home:
- Select tap/fade/hold map; single-line legends:
- Sleep, resume, Activity recreation and process restart:
- Controller conflict and uncertain-send recovery:
- HOME selection/withdrawal and Android settings/other-app escape:
- Korean/English/emoji readability and microphone transcript quality:
- Local button latency measurement method/results:
- Sustained-use comfort and battery measurement method/results:
- Failures, unsupported functions and steps to reproduce:

Label each result as automated, emulator, constrained-layout, physical button use, or not tested. A constrained viewport on RG Rotate does not certify another handheld. An APK build does not establish runtime compatibility.
