Captured agent-list envelope and initial observe-frame shape from local Herdr CLI/server 0.9.0 on 2026-09-14. IDs, paths, titles, states, and terminal content replaced with synthetic data. Stream closed record and control commands corroborated against the v0.9.0 public source; real isolated stream tests are recorded separately. These samples do not contain user terminal output.

# Plain-text snapshot contract

The installed CLI was also queried with `pane read ... --source recent-unwrapped --lines 160 --format text` against a real existing pane on 2026-09-14. Stdout is plain UTF-8 terminal output, not the socket API's JSON envelope. `pane-read.txt` is synthetic content matching this format; no real agent conversation is included.
